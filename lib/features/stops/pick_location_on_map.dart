import 'package:flutter/material.dart';

class PickLocationOnMap extends StatelessWidget {
  const PickLocationOnMap({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pick location')), 
      body: const Center(
        child: Text('Map picker not implemented'),
      ),
    );
  }
}

